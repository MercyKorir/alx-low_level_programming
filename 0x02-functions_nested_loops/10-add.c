#include <stdio.h>
#include "main.h"

/**
* add - adds two numbers and outputs the result
* @a: The argument to be operated on
* @b: The second argument to be operated on
* Return: outputs the sum of the two numbers.
*/
int add(int a, int b)
{
	int res;

	res = a + b;

	return (res);
}
