#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
* _abs - prints the absolute value for an integer
* @c: integer to be operated on
* Return: the absolute value is printed.
*/
int _abs(int c)
{
	int abs_c = abs(c);

	return (abs_c);
}
