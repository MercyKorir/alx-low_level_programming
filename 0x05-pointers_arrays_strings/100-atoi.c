#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
* int _atoi - converts a string to an integer
* @s: string to be converted
* Return: the integer is returned.
*/
int _atoi(char *s)
{
	return (atoi(s));
}
