#include <stdio.h>
#include "main.h"

/**
* mul - multiplies two numbers and returns the result
* @a: first integer
* @b: second integer
* Return: the result is returned.
*/
int mul(int a, int b)
{
	int res;

	res = a * b;

	return (res);
}
